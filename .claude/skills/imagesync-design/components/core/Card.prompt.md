Flat card — the only card style: mist (or petal) fill, 20px radius, zero elevation.

```jsx
<Card padding={14}>…status row…</Card>
<Card variant="emphasis" padding="14px 16px" onClick={openWizard}>Finish setup</Card>
```
