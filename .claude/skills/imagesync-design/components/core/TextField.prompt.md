Text field — mist-filled input with floating label and muted prefix icon; focus turns the border and label raspberry.

```jsx
<TextField label="Relay IP" icon="router" value={host} onChange={setHost} />
<TextField label="Pairing secret" icon="key" type="password" value={secret} onChange={setSecret} />
```
