Ripple rings radiating from a central orb — the success/connected confirmation motif.

```jsx
<RippleRings size={160}>
  <div style={{ width: 72, height: 72, background: 'var(--raspberry)', borderRadius: '50%', display: 'grid', placeItems: 'center' }}>
    <span className="material-icons" style={{ color: '#fff', fontSize: 32 }}>link</span>
  </div>
</RippleRings>
```

Also wraps a `MorphingBlob` for the send-clipboard success orb.
