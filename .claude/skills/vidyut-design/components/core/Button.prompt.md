Pill button — the app's only button shapes: full-width 54px stadium pills, plus raspberry text buttons.

```jsx
<PressableScale><Button icon="link" onClick={pair}>Pair manually</Button></PressableScale>
<PressableScale><Button variant="outlined" icon="qr_code_scanner">Scan QR</Button></PressableScale>
<Button variant="text" muted>Skip</Button>
```

Labels are 1–3 words, sentence case.
