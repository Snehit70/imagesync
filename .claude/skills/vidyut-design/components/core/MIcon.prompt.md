Material icon glyph — Vidyut's only icon system (Flutter's built-in Material Icons).

```jsx
<MIcon name="link" size={32} color="var(--raspberry)" />
<MIcon name="notifications_active" outlined size={28} color="var(--raspberry)" />
```

Common names: link, link_off, wifi_find, cloud_off, qr_code_scanner, dns, router, settings_ethernet, key, refresh, check_circle, check, error_outline, warning_amber_rounded→warning_amber, chevron_right, tune, ios_share, settings, bug_report, photo_library, battery_charging_full, delete_sweep, priority_high.
