Pulsing dot with expanding halo — indicates active searching (relay discovery, "Searching" status).

```jsx
<PulsingDot size={10} />
// inline before a label:
<div style={{ display: 'flex', alignItems: 'center', gap: 6 }}>
  <PulsingDot /> <span>Searching</span>
</div>
```
